-- script that compute the score average of all records in the second_table
-- of the database hbtn_0c_0, the result column should be average
-- the database name will be passed as command line argument.!
SELECT AVG(score) AS average FROM second_table;
exit
